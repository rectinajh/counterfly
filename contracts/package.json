{
  "name": "@counterfly/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "typecheck": "tsc --noEmit",
    "build": "hardhat compile",
    "deploy:cc3": "hardhat run scripts/deployCounterflyASC.ts --network cc3Testnet",
    "deploy:sepolia": "hardhat run scripts/deployRwaAction.ts --network sepolia",
    "deploy:receivable": "hardhat run scripts/deployRwaReceivable.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^4.9.6",
    "@types/node": "^20.14.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10",
    "typescript": "^5.5.4"
  }
}
